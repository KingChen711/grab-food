# Server-only — used by API route handlers to proxy to backend
API_URL=http://localhost:3001

# Client-side — Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
