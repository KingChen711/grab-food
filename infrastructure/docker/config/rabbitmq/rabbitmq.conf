# RabbitMQ Configuration for Grab Food Platform

# Memory & Disk
vm_memory_high_watermark.relative = 0.6
disk_free_limit.relative = 1.5

# Connection tuning
heartbeat = 60
channel_max = 256

# Logging
log.console = true
log.console.level = info
